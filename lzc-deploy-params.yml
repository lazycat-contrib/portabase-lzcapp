params:
  - id: timezone
    type: string
    name: "Timezone"
    description: "Timezone setting (for example, Asia/Shanghai or America/New_York)"
    default_value: "Asia/Shanghai"
    optional: false

locales:
  zh:
    timezone:
      name: "时区"
      description: "时区设置（例如：Asia/Shanghai 或 Europe/Berlin）"
